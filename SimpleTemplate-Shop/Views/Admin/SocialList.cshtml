@model IEnumerable<AppSocialAddress>

@{
    ViewBag.Title = "Control panel: Social Data";
    Layout = "_AdminLayout";
}

<br />
<div class="container">
    <table class="table">
        <thead>
            <tr>
                <th>Logo</th>
                <th scope="col">Social URL</th>
                <th class="text-right">Action</th>
            </tr>
        </thead>
        @foreach (var item in Model)
        {

            <tbody>
                <tr>
                    <th>
                        <img class="borderRadius border border-dark" src="~/images/@item.AppSocialImg" height="40" width="40" asp-append-version="true" />
                    </th>
                    <th>@item.UrlAddress</th>
                    <th class="text-right">
                        <form asp-action="DeleteSocialAddress" asp-route-id="@item.Id" method="post">
                            <a class="btn btn-outline-warning" asp-action="EditSocialData" asp-route-id="@item.Id">Edit</a>
                            <button type="submit" class="btn btn-outline-danger" onclick="return confirm('Delete: @item.UrlAddress?')">
                                Delete
                            </button>
                        </form>
                    </th>
                </tr>
            </tbody>
        }
    </table>
    <a asp-action="CreateSocialData" class="btn btn-outline-success" style="font-size:1.5em;">Add</a>
    <a asp-action="Index" class="btn btn-outline-primary" style="font-size:1.5em;">Back</a>
</div>