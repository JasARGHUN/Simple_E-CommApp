@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

@{
    ViewBag.Title = "Role panel";
    Layout = "_AdminLayout";
}

<br />
<div class="container">
    <br />

    <p class="mt-3 text-center" style="font-size:2em; font-weight:bolder; text-decoration-line:underline;">Role List</p>

    <div class="card m-2 p-2">

        <table class="table" style="font-size:1.5em;">
            <thead>
                <tr>
                    <th scope="col">Role</th>
                    <th class="text-right" scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var role in Model)
                {
                    <tr>
                        <td>@role.Name</td>
                        <td class="text-right">
                            <form asp-action="Delete" asp-route-id="@role.Id" method="post">
                                <button type="submit" class="btn btn-sm btn-danger" style="font-size:1em;" onclick="return confirm('Delete: @role.Name?')">
                                    Delete
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="text-center">
            <a asp-action="Create" class="btn btn-success" style="font-size:1.3em;">Add role</a>
            <a asp-action="UserList" class="btn btn-warning" style="font-size:1.3em;">User Liset</a>
            <a asp-controller="Admin" asp-action="Index" class="btn btn-primary" style="font-size:1.3em;">Back</a>
        </div>
    </div>
</div>
